name: ebbot_dart_client
description: "The Ebbot dart client library."
version: 0.0.6
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
homepage:

environment:
  sdk: ">=3.2.6 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  socketcluster_client:
    git:
      url: https://github.com/ebbot-ai/socketcluster_client
      ref: main
  uuid: ^4.3.3
  json_annotation: ^4.8.1
  json_serializable: ^6.6.0
  logger: ^2.0.2+1
  get_it: ^7.6.8
  mockito: ^5.4.4
  version: ^3.0.2
dev_dependencies:
  flutter_lints: ^3.0.1
  build_runner: ^2.3.3
  integration_test:
    sdk: flutter

flutter:
  